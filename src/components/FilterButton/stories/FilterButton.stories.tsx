import * as React from "react";
import FilterButton from "../component";

// // // //

export default {
  title: "Components/FilterButton",
  component: FilterButton,
};

export const Render = () => <FilterButton />;
